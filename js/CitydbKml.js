define([], function () {
  "use strict";

  var CitydbKml = function (url, thematicDataUrl) {
    this.url = url;
    this.thematicDataUrl = thematicDataUrl;
  };

  CitydbKml.prototype.add = function (viewer) {

  };

  return CitydbKml;
});
